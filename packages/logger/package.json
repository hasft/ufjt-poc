{
  "name": "@ufjt-poc/logger",
  "version": "1.0.3",
  "type": "module",
  "main": "dist/bundle.js",
  "types": "dist/bundle.d.ts",
  "scripts": {
    "build": "swc src -d build",
    "watch": "pnpm build -- -w",
    "dev": "pnpm watch",
    "test": "jest",
    "lint": "eslint . --fix",
    "bundle": "ufjt-bundler bundle src/main.ts"
  },
  "dependencies": {
    "kleur": "^4.1.4"
  },
  "devDependencies": {
    "@ufjt-poc/bundler": "workspace:*"
  }
}