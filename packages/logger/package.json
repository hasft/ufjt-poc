{
  "name": "@ufjt-poc/logger",
  "version": "1.0.4",
  "type": "module",
  "main": "dist/bundle.js",
  "types": "dist/bundle.d.ts",
  "scripts": {
    "build": "swc src -d build",
    "watch": "pnpm build -- -w",
    "dev": "pnpm watch",
    "test": "jest",
    "lint": "eslint . --fix",
    "bundle": "ufjtcmd bundle src/main.ts --outdir=dist"
  },
  "dependencies": {
    "kleur": "^4.1.4"
  }
}